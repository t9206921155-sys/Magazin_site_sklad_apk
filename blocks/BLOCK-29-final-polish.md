# Блок 29 — Финальная доводка: гигиена, staging-автоматы, бэкенд RN

**Статус:** 🔨 в работе (открыт 12.09.2026)
**Сессия:** 12.09.2026
**Зависит от:** блоки 25, 28 — выполнены
**Оценка:** одна-две сессии

---

## Цель

Довести проект до ума: убрать накопившиеся шероховатости, автоматизировать
всё автоматизируемое из ручных блоков 17/18, реализовать серверную часть
ТЗ покупательского приложения (§6.2 — без внешних зависимостей).

## Задачи

### Ч1. Гигиена версий и скриптов
- [ ] Фолбэк 1.0.6→1.1.0 в `api.py`; параметризовать `release_status.sh` (+ Shop)
- [ ] 1.0.6→1.1.0 в операционных доках (исторические не трогать)
- [ ] `mobile/build-apk.sh`: заглушка → редирект; `mobile/README.md`; честный `package.json`
- [ ] `install.sh` → обёртка над `setup.sh`
- [ ] `.env.example`: пустые BOT_TOKEN/ADMIN_IDS вместо фейка
- [ ] `--help` и защита от флагов в обоих `build-apk.sh`

### Ч2. Lock-файл и staging-автоматизация
- [ ] `telegram-shop/requirements-lock.txt` из чистого venv
- [ ] `deploy/backup-drill.sh` (авто-часть блока 18) + тесты
- [ ] `deploy/staging-report.sh` (авто-часть блока 17) + тесты
- [ ] Документация: BLOCK-18 примечание, SETUP-AUTO.md раздел приёмки

### Ч3. Бэкенд покупательского приложения (ТЗ §6.2)
- [ ] `GET /api/product/{id}` — карточка одним запросом
- [ ] `POST /api/mobile/register` + таблица `mobile_devices`
- [ ] FCM-хуки (dry-run gated, как остальные провайдеры) + диагностика
- [ ] `GET /api/app/version?platform=` (обратная совместимость)
- [ ] `FCM_CREDENTIALS_JSON` в config + `.env.example` + `setup-env.sh`
- [ ] `tests-block29.py` + регистрация в `run-tests.sh` и ROADMAP
- [ ] ANDROID-APP-TZ §6.2 и BLOCK-25: пометить серверную часть ✅

### Ч4. Финал
- [ ] PR ветки в `main`
- [ ] ROADMAP: блок 29 ✅, счётчики
- [ ] Финальная синхронизация (CI-лог подтянут)

## Критерии приёмки

- [ ] `pytest tests/` зелёный (45+ тестов)
- [ ] Полный `run-tests.sh` зелёный (включая новый сюит)
- [ ] `backup-drill.sh` проходит на синтетической и реальной БД
- [ ] Ни одного `1.0.6` вне исторических документов и `blocks/`
- [ ] PR открыт, CI на ветке зелёный

## Отчёт

> Заполняется по завершении.
