# Блок 05 — Гигиена репозитория

**Статус:** ✅ выполнено
**Сессия:** 04.09.2026
**Коммит:** `7c2ca12`

---

## Цель

Уменьшить вес репозитория, убрать неиспользуемые файлы и кэш.

---

## Отчёт

**Скриншоты.** Все 164 файла в `telegram-shop/screenshots/` сверены со всеми
`.md` / `.py` / `.js` / `.html` / `.xml` / `.json` репозитория:

| | |
|---|---|
| Используются в документации | **96 PNG** — оставлены |
| Скрипты-генераторы `shoot_*.py`, `make_shots.py` | **11** — оставлены |
| Ноль ссылок где-либо | **57 PNG** — удалены, **−22 МБ** |

Дополнительно проверено, что ни один из 57 не создаётся shoot-скриптами.
`site-preview.png` упоминается в README — не тронут.

Каталог `screenshots`: 44 МБ → 22 МБ.

**Кэш.** Удалены все `__pycache__` и `.pyc`, выполнен
`git gc --prune=now --aggressive`. `.gitignore` уже был корректен — мусор
в индекс не попадал.

**Права на скриптах.** `git add -A` снял флаг исполняемости с 7 скриптов
(в истории уже был коммит «Restore executable flags» — грабли повторяющиеся).
Возвращено через `git update-index --chmod=+x` до коммита.

---

## Осталось (не делать без согласия владельца)

`.git` весит **74 МБ** — история хранит все версии удалённых картинок.
Вычистить можно только через `git filter-repo` с перезаписью истории и
force-push. Это **ломает все существующие клоны**, поэтому не выполнялось.

Обходной путь для песочницы — sparse-клон на 7 МБ, описан в
`SESSION-PLAYBOOK.md`.
