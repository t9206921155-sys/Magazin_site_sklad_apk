# Блок 16 — Marketplace 2.0: продавцы, сделки и доверие

**Статус:** ✅ выполнено (08.09.2026)

## Цель
Закрыть незавершённые функции `MARKETPLACE-PLAN.md` после стабилизации инфраструктуры.

## Задачи
- [x] верификация продавца и бейдж доверия — была реализована ранее
      (`verification_status`, подача в кабинете, `POST /admin/api/sellers/{sid}/verify`,
      бейдж в кабинете и карточке; аудит блока 09)
- [x] подписки на магазины и лента новинок — НОВОЕ: `store_subscriptions` (UNIQUE
      seller+user), `POST /api/sellers/{slug}/subscribe|unsubscribe`,
      `GET /api/my/subscriptions` (витрины + последние новинки каждой)
- [x] тарифы витрин и лимиты объявлений — были реализованы ранее
      (`seller_plans`, лимиты с 429, запрос смены плана; аудит блока 09)
- [x] бронь и переговоры о цене — переговоры были (`/api/offers`, countered);
      бронь НОВОЕ: `reservations` (TTL 48ч, проверка остатка, отмена покупателем,
      авто-экспирация)
- [x] жалобы, модерация и чёрный список — НОВОЕ: `complaints` (6 причин, текст ≥10
      символов), очередь модерации `/admin/api/complaints`, решение с опциональным
      `ban_seller`, `/admin/api/sellers/{sid}/ban` (banned → исчезает из выдачи и
      витрин, разбан возвращает); текстовая модерация контента была (banned words)
- [x] escrow/безопасная сделка — было: `held_balance` + авторазморозка
      `escrow_days`; деньги продавцу не попадают до истечения холда
- [x] seller analytics: просмотры, конверсия, ответы — НОВОЕ: счётчик просмотров
      витрины (`sellers.views`, инкремент на `/seller/{slug}`, виден админу);
      базовая статистика (товары/заказы/продажи/баланс) и процент ответов были
- [x] продвижение объявлений и аудит действий — НОВОЕ: «поднять объявление»
      (`products.boosted_until`, 24ч, cooldown 7 дней, только свой товар);
      поднятые — вверху выдачи по умолчанию, явная сортировка приоритетнее;
      аудит действий — wh_log (был), бан журналируется

## Приёмка
- [x] состояние сделки явно хранится и журналируется — orders.history +
      sellers_escrow/released; статусы offers/reservations/complaints в БД
- [x] продавец не может получить деньги до подтверждения условий —
      эскроу-холд держит сумму в `held_balance` до истечения `escrow_days`
- [x] права buyer/seller/admin покрыты API-тестами — секции тестов 1/2/3/5/6:
      buyer (подписки, брони, жалобы), seller (буст только своих, 403 без ключа),
      admin (модерация, бан, разбан, 403/404 на чужие действия)

## Отчёт (сессия 08.09.2026) — блок закрыт

### Реализовано
- `store.py`: таблицы `store_subscriptions`, `reservations`, `complaints`;
  колонки `sellers.views`, `products.boosted_until`; методы подписок (идемпотентных),
  брони (TTL, остатки, права), жалоб (нормализация причин, резолюции),
  `ban_seller` (без затирания in_stock — видимость через статус), `seller_track_view`,
  `boost_product` (cooldown).
- `api.py`: 9 эндпоинтов (subscribe/unsubscribe/my subscriptions, reservations
  create/list/cancel, complaints create + admin list/resolve, ban/unban, boost);
  boost-приоритет в `_apply_catalog_filters` (только при отсутствии sort);
  просмотры витрины на SSR `/seller/{slug}`.
- Найден и исправлен по ходу: `store_unsubscribe` возвращал `subscribed: True`
  после успешного удаления (сейчас False + `was_subscribed`).

### Тесты (`tests-block16.py` — 45/45)
- подписки: дубликаты, 422 на коротком ключе, лента новинок, отписка, 404;
- брони: TTL, остатки (422), чужая отмена (403), своя отмена, 404;
- жалобы: создание, привязка к продавцу, 422 без текста, админ-очередь (403 без
  токена), решение + бан → товар из выдачи, витрина 404, разбан → всё вернулось;
- просмотры копятся; буст: 24ч, повтор → 409, чужой товар нельзя, порядок выдачи,
  приоритет явной сортировки; границы прав seller/admin/buyer.
- Регрессия: labels 20/20, hid 8/8, stage5 23/23, block06 33/33, block09 47/47,
  block13 33/33, block14 32/32, contracts 32/32, block15 40/40, pytest 16/16.

### Не входит в блок (внешние условия)
- Оплата тарифов и платного продвижения (биллинг) — при появлении платёжки.
- ИИ-скрининг жалоб на запрещённое — текстовая модерация есть, фото-скрининг в бэктлоге 3.8.
