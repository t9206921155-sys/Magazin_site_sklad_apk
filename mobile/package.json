{
  "name": "telegram-shop-mobile-prototype",
  "version": "0.0.0",
  "private": true,
  "description": "АРХИВНЫЙ прототип RN-экранов (mobile/src) — не собирается. Актуальное приложение: mobile/android-wrapper (см. mobile/README.md)",
  "scripts": {
    "start": "echo 'АРХИВ: src/ — прототип, сборке не подлежит. Сборка актуального приложения: ./android-wrapper/build-apk.sh [URL]' && exit 1",
    "build:apk": "bash build-apk.sh"
  }
}
