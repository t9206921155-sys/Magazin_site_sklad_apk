# Local runtime smoke — 2026-09-03

Продолжение сессии от 2026-08-31 (`LOCAL-RUNTIME-SMOKE-2026-08-31.md`).

## Environment
- Запуск из `telegram-shop/bot.py`, бот отключён (`BOT_TOKEN` пустой).
- `.env` создан из `.env.example`, `BOT_TOKEN` очищен.
- Зависимости из `requirements.txt` установлены полностью (aiogram, fastapi, boto3, qrcode, webauthn, pywebpush и др.).
- База — локальный SQLite, режим склада `db_mode = vps`.

## Результат: проект поднимается и работает

Публичные маршруты:

| Маршрут | Код |
|---|---|
| `/` | 200 |
| `/catalog` | 200 |
| `/shop` | 200 |
| `/app` (Mini App) | 200 |
| `/admin` | 307 (редирект на логин — штатно) |
| `/warehouse/` | 200 |
| `/warehouse/app.js` | 200 |
| `/sitemap.xml` | 200 |
| `/robots.txt` | 200 |

Регрессий по итогам прошлой сессии не обнаружено: `marketplace_settings()`
и `upsert_product_with_id()` на месте, корень сайта не падает.

## Склад: авторизация и API

- `POST /api/warehouse/login` (`admin` / `ADMIN_PASSWORD`) -> **OK**, выдаёт `X-WH-Token`.
- Без токена защищённые эндпоинты корректно отдают **403**.

С токеном:

| Эндпоинт | Код |
|---|---|
| `GET /api/warehouse/products` | 200 |
| `GET /api/warehouse/products/{pid}` | 200 |
| `POST /api/warehouse/products` | 201 |
| `PUT /api/warehouse/products/{pid}` | 200 |
| `GET /api/warehouse/cloud/status` | 200 |
| `GET /api/warehouse/scans` | 200 |
| `GET /api/warehouse/sync` | 200 |
| `GET /api/warehouse/settings` | 200 |

Режимы сканирования `POST /api/warehouse/scan` — все **200**:
`search`, `receive`, `sell`, `inventory`. Записи корректно попадают в журнал сканов.

`cloud/status` отдаёт `db_mode: vps`, `storage_preset: yandex` — трёхрежимная
схема из прошлой сессии цела.

## Найденный и исправленный баг: удаление товара со склада

**Симптом.** Кладовщик не мог удалить товар из интерфейса склада.

**Причина — две несогласованности:**

1. Эндпоинта `DELETE /api/warehouse/products/{pid}` **не существовало** — прямой
   вызов возвращал **405 Method Not Allowed**. Были только `archive` и `bulk`.
2. UI склада (`warehouse/app.js`, функция `delProduct`) ходил в
   `DELETE /admin/api/products/{pid}` — а этот маршрут защищён `require_admin()`
   и требует **админ-токен магазина**. Кладовщик со своим `X-WH-Token`
   получал **403 «Нет доступа»**.

Воспроизведено до правки: создание товара -> `DELETE /admin/api/products/{id}`
с `X-WH-Token` -> `403 {"detail":"Нет доступа"}`.

**Исправление:**

- `api.py`: добавлен эндпоинт `DELETE /api/warehouse/products/{pid}` рядом с
  `archive`. Требует роль `admin` склада, проверяет существование товара,
  пишет запись в журнал действий через `store.wh_log_add`.
- `warehouse/app.js`: `delProduct()` теперь вызывает
  `/api/warehouse/products/{id}` вместо `/admin/api/products/{id}`.
  Ветка `isDirectMode()` (Supabase direct) не тронута.

**Проверка после правки:**

| Сценарий | Ожидание | Факт |
|---|---|---|
| `DELETE` существующего товара | 200 | **200** `{"ok":true,"id":11}` |
| `GET` удалённого товара | 404 | **404** |
| `DELETE` несуществующего id | 404 | **404** |
| `DELETE` без токена | 403 | **403** |
| Запись в журнале | есть | **есть** — «удалил товар Del тест 2 (id 11)» |

Синтаксис проверен: `api.py` — `ast.parse` OK, `warehouse/app.js` — `node --check` OK.

## Cleanup
- Все временные smoke-товары удалены.
- Настройки склада не менялись (оставлен режим `vps`).

## Не покрыто (как и в прошлый раз)
- Реальные креды Supabase и Yandex Object Storage.
- Ручной UX на живом Android-устройстве.
- Настоящая камера / нативный сканер на телефоне.
- Платёжные вебхуки с реальными провайдерами.

---

# Продолжение сессии 2026-09-03: аудит связности + Этап 3 (HID-сканер)

## 1. Сплошной аудит «UI зовёт — API отвечает»

После находки с удалением товара прогнал автоматическую сверку всех
клиентских вызовов против зарегистрированных маршрутов FastAPI.

| Файл | Вызовов | Битых |
|---|---|---|
| `warehouse/app.js` | 36 | **0** |
| `admin/app.js` | 52 | **0** |
| `webapp/app.js` | 16 | **0** |

Кандидаты `/admin/api/export/{kind}`, `/admin/api/reports/sellers[.pdf]`,
`/admin/api/orders/{id}/...`, `/admin/api/reviews/{id}`, `/admin/api/sellers/{id}/...`
проверены вручную — это конкатенация id и query-строк, все маршруты существуют.

**Вывод:** баг с удалением товара был единственным рассогласованием UI↔API.

## 2. Этап 3: HID-приём Bluetooth/ТСД доведён до рабочего

Было помечено в коде и в меню как «Заглушка (stub)». Разбор показал, что
проблема не в полноте, а в **неверной логике парсера** — четыре дефекта:

1. **Мёртвая ветка.** `if (delta < 150 && delta > 5) {...} else {...}` — обе ветки
   выполняли идентичный код, то есть проверка скорости ввода ни на что не влияла
   и ручной ввод человека тоже копился в буфер сканера.
2. **Склейка кодов.** Если сканер не слал Enter, буфер никогда не очищался, и
   следующий скан дописывался к предыдущему (`1111...` + `2222...`).
3. **Нет поддержки сканеров без Enter-суффикса** — такой код не отправлялся вообще.
4. **Двойное срабатывание** при дребезге: повторный скан того же кода уходил дважды
   (в режиме `sell` это списало бы товар два раза).

**Реализовано:**

- отсечка ручного ввода по межсимвольному интервалу (`FAST_MS = 35 мс`);
- сброс буфера при паузе — коды больше не склеиваются;
- поддержка сканеров **без** Enter: отправка по тишине (`IDLE_FLUSH = 120 мс`),
  переключатель «Сканер шлёт Enter в конце» в интерфейсе;
- дедупликация повторного кода в окне `DEDUP_MS = 700 мс`;
- отсечка шума короче 4 символов;
- живой лог принятых кодов, кнопка вкл/выкл приёма, показ текущего режима
  (Поиск / Приёмка / Продажа / Инвентаризация);
- из меню и комментариев убрано слово «заглушка».

**Тесты логики парсера** (`node`, 8 проверок) — все проходят:

| Сценарий | Итог |
|---|---|
| Сканер с Enter-суффиксом | ✅ |
| Два скана подряд не склеиваются *(был баг)* | ✅ |
| Скан без Enter не оставляет мусор *(был баг)* | ✅ |
| Медленный ручной ввод не считается сканом *(был баг)* | ✅ |
| Дедупликация повторного скана *(был баг)* | ✅ |
| Повтор после паузы принимается снова | ✅ |
| Сканер без Enter — отправка по паузе | ✅ |
| Короткий шум отброшен | ✅ |

## 3. Регрессия после правок

Сервер перезапущен, `node --check warehouse/app.js` — OK.

Маршруты `/`, `/catalog`, `/shop`, `/app`, `/warehouse/`, `/warehouse/app.js`,
`/sitemap.xml` — **200**. Склад: `products`, `scans`, `cloud/status`, `log` — **200**.
Все четыре режима `POST /api/warehouse/scan` — **200**. Удаление товара — **200**.

## Не покрыто
- Проверка на реальном Bluetooth-сканере (Zebra DS3678, Атол SB2108) — нужен физический ТСД.
- Реальные креды Supabase / Yandex Object Storage.
- Ручной UX на живом Android-устройстве.

---

# Продолжение 2026-09-04: Этап 4 (печать)

## Найденные баги

### 1. ZPL-инъекция через название товара (критично для принтера)

В ZPL символы `^` и `~` начинают команду. Название вида `Кабель ^XZ 3" ~JA`
подставлялось в шаблон без очистки, поэтому:

- `^XZ` **обрывал этикетку** посреди названия — печатался мусор;
- `~JA` — это команда **отмены всех заданий печати** на принтере Zebra.

Товар с такими символами в названии мог остановить печать на всём складе.

Исправлено: `_zpl_escape()` вырезает `^`, `~`, `\`, переводы строк.
Для EPL добавлен `_epl_escape()` — там строки в кавычках, экранируются `"` и `\`.

### 2. Печать из интерфейса всегда давала 403

`printByFilter()`, `exportOne()` и PDF-ветка `printWithPrinter()` открывали
ссылку через `window.open(...)`. Браузер при этом **не отправляет** заголовок
`X-Wh-Token`, а эндпоинты печати требуют авторизации.

Проверено до правки: `GET /api/warehouse/labels.pdf?loc=A-01` без заголовка -> **403**,
с заголовком -> **200**.

Исправлено: добавлен хелпер `downloadAuthed()` — тянет файл через `fetch`
с заголовками и отдаёт как blob (с фолбэком на скачивание, если popup заблокирован).
Все три места переведены на него.

### 3. Мелочи
- `copies=0` в EPL давал `P0,1` (принтер не печатает ни одной этикетки) -> `max(1, copies)`.
- Цена выводилась как `1234.5` вместо `1234.50`.

## Новое: ценники для торгового зала

`GET /api/warehouse/price-tags.pdf` — то, чего не хватало по пункту «пакетная
печать ценников». Отличие от `labels.pdf`: та печатает складскую наклейку с
упором на штрих-код, здесь — ценник для покупателя.

- крупная цена с автоподбором кегля под ширину этикетки;
- старая цена перечёркнутой + рассчитанный процент скидки (если задан `old_price`);
- QR со штрих-кодом, название магазина, SKU и место хранения;
- раскладка сеткой на A4 под заданный размер, многостраничность, до 9 копий;
- выбор товаров по `ids` **или** по фильтру `cat` / `loc` / `owner`;
- запись в журнал действий склада.

В UI склада добавлена кнопка «🏷 Ценники для зала (PDF)».

## Тесты

`tests-labels.py` — **20/20 passed**: ZPL-инъекция, EPL-экранирование,
формат цены, copies, PDF-наклейки, ценники (с QR и без, многостраничные,
пустой список -> ValueError), длинное название.

`tests-hid-scanner.js` — **8/8 passed** (регрессия, без изменений).

## Регрессия

Страницы `/`, `/catalog`, `/shop`, `/app`, `/warehouse/`, `/sitemap.xml` — **200**.
Склад: `products`, `scans`, `log`, `printers`, `cloud/status` — **200**.
Печать: `labels.pdf`, `labels.prn` (zpl/epl), `price-tags.pdf` — **200**.
Сканирование во всех 4 режимах — **200**. Удаление — **200**.
Без токена ценники — **403**, без параметров — **422**.

## Замечание по ценам (не баг)

Цены хранятся как **целые рубли** (`int(...)` в `store.py`) — это сквозное
решение всего проекта: заказы, комиссии, выплаты. Копейки на этикетке
поддержаны в форматтере, но из базы приходит целое. Менять тип цены —
отдельная задача с миграцией БД, в рамках Этапа 4 не трогал.

## Не покрыто
- Печать на реальном Zebra/Eltron (нужно железо).
- Реальный Bluetooth-сканер.
- Креды Supabase / Yandex Object Storage.

---

# Продолжение 2026-09-04: Этап 5 (офлайн + 1С)

## 1. Критичный баг: service worker кэшировал API-ответы

`warehouse/sw.js` применял **cache-first ко всем GET-запросам**, включая
`/api/warehouse/*`. Последствия на живом складе:

- после первой загрузки список товаров и **остатки переставали обновляться** —
  отдавалась копия из кэша;
- кладовщик видел неактуальные остатки и мог продать то, чего нет;
- данные не обновлялись, пока не сбросишь кэш браузера;
- в кэш попадали ответы `/api/warehouse/settings` — **с ключами облака**.

### Переписано

| Тип запроса | Было | Стало |
|---|---|---|
| Оболочка (HTML/JS/иконки) | cache-first | cache-first (правильно) |
| `GET /api/...` | **cache-first** | **network-first**, кэш только как аварийный фолбэк |
| `POST/PUT/DELETE /api/...` | не обрабатывались | очередь в IndexedDB + Background Sync |
| Настройки, логин, ключи | кэшировались | `isSensitive()` — не кэшируются |

Кэш разделён на `SHELL_CACHE` и `API_CACHE`, версия поднята до `wh-v2`
(старый `wh-v1` удаляется при активации).

### Офлайн-очередь

Изменяющие операции без сети складываются в IndexedDB и отдают `202
{queued: true}`. При появлении сети отправляются автоматически (Background
Sync, тег `wh-sync`, плюс ручной flush по событию `online`).
Ответы `4xx` снимаются с очереди (повторять бессмысленно), сетевые ошибки —
остаются до следующей попытки.

В UI: индикатор в шапке меняет цвет и текст —
«📴 Нет сети — работаем офлайн · в очереди: N», «⏳ Отправляем отложенные
операции», «⚠️ Данные из кэша». Ответ `X-From-Cache: 1` подсвечивается,
чтобы кладовщик не принял устаревшие данные за актуальные.

## 2. Обмен с 1С: не было выгрузки остатков

Существовали `/1c/catalog`, `/1c/orders`, `/1c/orders/ack`, `/1c/orders/status`,
но **не было способа обновить только остатки**. `POST /1c/catalog` идёт через
`importer.apply_import()`, который пропускает позиции без `name` и создаёт
новые товары — то есть для регулярной синхронизации остатков 1С была обязана
слать полный каталог целиком.

### Добавлено

- `POST /1c/stock` — `{"items": [{"code": "НМ-001", "stock": 12, "price": 990}]}`
  Обновляет только `stock` / `price` / `old_price` / `in_stock` у **существующих**
  позиций. Названия, описания и фото не трогает, новые товары не создаёт.
  Возвращает `{updated, not_found}` — 1С видит, каких кодов нет на витрине.
- `GET /1c/stock` — текущие остатки для сверки на стороне 1С.
- `Store.update_stock_by_code()` — поиск по `code`, фолбэк на `name`;
  если прислан только остаток, `in_stock` выводится из него (0 -> снят с продажи).

Ограничения: максимум 5000 позиций за вызов (413), нечисловые значения
игнорируются без падения, токен обязателен.

## 3. Тесты

`tests-stage5.py` — **23/23 passed**: обновление остатка и цены, сохранность
названия, `stock=0` -> `in_stock=false`, частичный успех с неизвестным кодом,
валидация (403/422/413), видимость изменений на складе, целостность старых
1С-маршрутов, проверка стратегий в `sw.js`.

Регрессия: `tests-labels.py` **20/20**, `tests-hid-scanner.js` **8/8**.
Итого по проекту — **51 автотест**, все проходят.

Страницы `/`, `/catalog`, `/shop`, `/app`, `/warehouse/`, `/warehouse/sw.js`,
`/sitemap.xml` — 200. Склад, печать (PDF/ZPL/ценники), все 4 режима
сканирования — 200.

## Не покрыто
- Реальная проверка офлайн-очереди на устройстве (нужен браузер с эмуляцией
  обрыва сети; логика покрыта тестами, но UX на телефоне не проверялся).
- Background Sync не поддерживается в Safari/iOS — там очередь отправится
  при следующем открытии приложения (фолбэк на событие `online` работает).
- Реальная 1С-база: проверено на моках, боевую выгрузку нужно прогнать на стенде.
