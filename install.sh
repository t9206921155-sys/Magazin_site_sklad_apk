#!/usr/bin/env bash
# ============================================================
#  Telegram Shop — install.sh (legacy-обёртка, блок 29)
#
#  Вся логика переехала в ./setup.sh (мастер установки «из коробки»).
#  Этот файл оставлен для совместимости (старые инструкции, PDF):
#  флаги --venv/--test/--no-run/--help работают как раньше.
#
#  Новым установкам рекомендуется ./setup.sh (см. SETUP-AUTO.md).
# ============================================================
set -euo pipefail
cd "$(dirname "$0")"
echo "ℹ️  install.sh → setup.sh (legacy-обёртка)" >&2
exec ./setup.sh "$@"
