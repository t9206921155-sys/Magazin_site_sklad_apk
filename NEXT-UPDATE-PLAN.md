# 📋 План следующего обновления — СОГЛАСОВАНО И РЕАЛИЗОВАНО

> Дата: 10.09.2026 · Статус: ✅ согласовано владельцем 11.09.2026 (вариант B; пакет
> `ru.telegramshop.shop`; общий keystore со «Складом»; buyer-APK в дистрибутив не
> включать) и реализовано в тот же день — см. отчёты `blocks/BLOCK-25-mobile-buyer-app.md`
> (Фаза 1) и `blocks/BLOCK-27-csp-hardening.md`
> Исходная точка: `main` = `48c5dbc` (PR #4 влит: блок 09 + блок 26 + дистрибутив 1.1.0)
> Документы-основания: `ROADMAP.md`, `IMPLEMENTATION-QUEUE.md`, `mobile/ANDROID-APP-TZ.md`, `blocks/BLOCK-25-mobile-buyer-app.md`

---

## 1. Где проект сейчас

Все кодовые блоки, выполнимые в песочнице, **закрыты**: 00–10, 12–16, 19–21, 23, 24, 26.
Складской APK 1.1.0 собран и раздаётся, дистрибутив `Telegram-Shop-1.1.0.zip` готов.

Остаток работ:

| Что | Статус | Можно ли сделать в песочнице |
|---|---|---|
| **Блок 25, Фаза 1** — покупательское Android-приложение (WebView-обёртка) | ⏳ | ✅ да — единственный оставшийся кодовый блок без внешних зависимостей |
| CSP (вынос inline-JS фронтов) — бэклог блока 15 | бэклог | ✅ да |
| Блок 17 — ручная staging-валидация | ⏳ | ❌ нужно окружение/credentials владельца |
| Блок 18 — backup/restore на тестовом VPS | ⏳ | ❌ нужен VPS |
| Блок 11 — реальное железо (ТСД, принтеры) | ⛔ | ❌ нужно железо |
| Блок 22 — Wildberries | ⛔ | ❌ ждёт официальный API-аудит WB |
| Скриншоты staging для фильтров (P1 очереди) | ⏳ | ❌ нужен staging URL |
| Блок 25, Фазы 2–6 — React Native-приложение | ⏳ | частично, ~4–5 недель, отдельные сессии |

**Вывод:** следующее обновление — **Блок 25, Фаза 1** (рекомендую), опционально + CSP-закалка.

---

## 2. Предлагаемый объём обновления (выберите вариант)

| Вариант | Состав | Оценка | Комментарий |
|---|---|---|---|
| **A — рекомендуется** | Блок 25 Фаза 1: покупательское APK | 1 сессия | Быстрый результат «как у Склада»: иконка, deep links, прямая раздача |
| **B** | Вариант A + CSP (вынос inline-JS, включение Content-Security-Policy) | 1–1,3 сессии | Закрывает последний бэклог безопасности блока 15 |
| **C** | Только подготовка ручных блоков: чек-лист 17/18 «под ключ», шаблон съёмки скриншотов фильтров | 0,5 сессии | Если APK пока не нужен |

---

## 3. Детальный план работ — Блок 25, Фаза 1 (вариант A)

### Шаг 1. Каркас обёртки
- Копия проверенного `telegram-shop/apk-build/android` → `mobile/android-wrapper`.
- Пакет **`ru.telegramshop.shop`**, deep link **`shop://connect?url=…`** (+ `shop://install`).
- WebView грузит адаптивный сайт `/` (каталог, корзина, оплата — всё уже на сайте).
- Splash/иконка покупательского стиля; кнопка «Повторить» на экране ошибки сети.
- Проверка обновлений при старте → мягкий баннер (по образцу складского update-check).

### Шаг 2. Сборка
- Параметризовать `apk-build/rebuild-apk.sh` (пакет / стартовый URL / имя приложения) —
  чтобы складской и покупательский APK собирались одним скриптом.
- **Отдельный keystore** для `ru.telegramshop.shop` (не складской); пароль — владельцу, в git не кладётся.
- Buyer-APK `Shop-1.0.0` (versionCode 1), minSdk как у складского (Android 8+).

### Шаг 3. Серверная часть
- Страница **`/download/app`** с QR-кодом (по образцу существующей `/download/android`).
- **`GET /api/app/version`** — текущая версия покупательского APK + `min_supported_version`.
- Раздача файла через `/apk/shop/` (как у Склада), запись в `/api/releases`.

### Шаг 4. Тесты и регрессия
- Новый `telegram-shop/tests-block25.py`: страница /download/app, version API,
  раздача APK, отсутствие секретов в настройках, корректные ответы при пустом каталоге.
- Полная регрессия в порядке из ROADMAP (…→ block16 → block19 → block23 → pytest → block15 последним).

### Шаг 5. CI
- GitHub Actions: собрать buyer-APK артефактом по образцу `ci-build-apk.sh`
  (сборка складского уже автоматизирована — переиспользуем механизм).

### Шаг 6. Документация
- Отчёт в `blocks/BLOCK-25-mobile-buyer-app.md` (раздел «Фаза 1»), статус в ROADMAP → ✅ Ф1.
- README: раздел «Приложение покупателя»; решение по включению APK в `build-distr.sh` — см. вопрос 4.

### Шаг 7. Git
- Работа в ветке сессии `arena/01a08b6f-magazin-site-sklad-apk` → коммиты → **PR в `main`** → влив после вашей проверки.

---

## 4. Критерии приёмки Фазы 1

- [ ] APK собирается воспроизводимо из CI; подписан; секреты не в git.
- [ ] Устанавливается на Android 8/12/14 *(установка на реальные устройства — за владельцем, инструкция приложу)*.
- [ ] Сквозной заказ на сайте открывается в приложении; оплата-редирект возвращается по deep link.
- [ ] Без сети — экран ошибки с «Повторить», без крашей.
- [ ] Регрессия сервера зелёная, `tests-block25.py` проходит.
- [ ] Обновлены ROADMAP, блок-отчёт, README.

---

## 5. Что нужно решить вам (Фаза 0)

1. **Объём**: вариант A, B или C из §2?
2. **Пакет** `ru.telegramshop.shop` — подтверждить?
3. **Keystore**: новый отдельный для shop (рекомендую) или общий со «Складом»?
4. **Дистрибутив**: класть ли buyer-APK в `Telegram-Shop-*.zip`, или он идёт только через сайт/CI?
5. **FCM/RuStore** — подтвердить перенос на Фазы 5–6 (в Фазе 1 не нужны).
6. *(Опционально)* staging URL — тогда добавлю реальные скриншоты фильтров (закроет ручной пункт P1).

---

## 6. Ограничения и риски

- Установку на физические устройства и реальную оплату выполняет владелец — в песочнице устройства нет; со своей стороны гарантирую сборку, автотесты и пошаговую инструкцию установки.
- WebPush в WebView нестабилен — пуши сознательно отнесены к Фазе 5 (React Native), в Фазе 1 их не обещаем.
- Имя пакета после публикации менять нельзя — поэтому фиксируем его до сборки.
- Цена/копейки остаются целыми рублями (общесистемное ограничение, вне рамок этого обновления).

---

## 7. Что дальше после Фазы 1

- **Фазы 2–6 (React Native/Expo)** — нативный UI, офлайн-корзина, FCM-push, RuStore: ~4–5 недель, поблочно по сессиям (план и критерии уже описаны в `mobile/ANDROID-APP-TZ.md` §10).
- Параллельно силами владельца: блоки 17/18 на staging/VPS, 11 на железе, 22 после аудита WB.

---

## 8. Порядок запуска (после вашего «согласовано»)

1. Вы отвечаете на вопросы §5.
2. Я фиксирую план в `blocks/BLOCK-25-mobile-buyer-app.md` (+ ROADMAP: блок в 🔨).
3. Реализация по шагам §3, тесты, регрессия.
4. PR в `main`, отчёт, обновление трекера.
